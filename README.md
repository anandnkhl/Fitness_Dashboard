This is the FrontEnd part of the Fitness Tracker.
It fetches the data from Postgres DB using GraphQL API
It uses React to create the UI of the dashboard.

## Steps for installation

`npm install`

`npm start`